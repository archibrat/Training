def growing_plant(upSpeed, downSpeed, desiredHeight):
    day = 0
    h = 0
    while h <= desiredHeight:
        h += upSpeed
        day += 1
        if h <  desiredHeight:
            h -= downSpeed
        else:
            return day
